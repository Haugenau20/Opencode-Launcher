---
description: "Run one stage of the live OpenCode workplace tour. Presenter-paced — one stage per invocation, then it stops. Pass a stage number 1-7, optionally followed by an argument such as a Jira key or a PR reference. No argument prints the menu."
---

Run the demo tour. Requested stage (may be empty): $ARGUMENTS

Load the **`demo-stages`** skill and follow it exactly. It carries the rules, the
output format, and the seven stage definitions.

Do not restate the skill, do not summarise it, and do not print anything before
the stage header. Run the single requested stage, then stop.
